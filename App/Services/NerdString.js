export default class NerdString {
    numericSectorFromUnit(unit){
        return parseInt(unit);
        // const numericSector = unit.match(/-?\d+/);
        // if (Array.isArray(numericSector))
        //     return parseInt(numericSector[0]);
        // return null;
    }
}